# ERRORS.md

작업 중 만난 오류·함정과 그 해결책을 기록한다. 같은 문제를 두 번 겪지 않기 위한 문서.
새 항목은 맨 위에 추가한다.

<!-- 형식 예시
## YYYY-MM-DD — 한 줄 증상
- **증상:** 무엇이 어떻게 잘못됐는지 (에러 메시지 포함)
- **원인:** 실제 원인
- **해결:** 어떻게 고쳤는지 (파일·명령 포함)
-->

## (알려진 함정) — 인라인 색상 변경 후 다크모드에서 색이 안 바뀜
- **증상:** 요소의 인라인 `color`/`background`/`border`를 바꿨더니 라이트모드는 정상인데 다크모드에서 예전 색 또는 대비가 깨진 채로 표시됨.
- **원인:** 다크모드가 `index.css`의 `html[data-dark] [style*="rgb(...)"]` 속성 선택자로 동작하는데, 새 인라인 값에 대응하는 오버라이드 규칙이 없어서.
- **해결:** `src/index.css`에 브라우저가 직렬화하는 정확한 형태(`rgb(r, g, b)`, 쉼표 뒤 공백 포함)로 `html[data-dark] [style*="..."]` 규칙을 추가한다.

## (알려진 함정) — meal update 스크립트가 CP949 콘솔에서 한글 깨짐
- **증상:** `scripts/*.ps1` 안에 한글을 직접 넣으면 CP949 Windows 환경에서 깨지거나 실행 오류.
- **원인:** 스크립트 본문은 의도적으로 ASCII 전용으로 유지됨.
- **해결:** 한글 텍스트는 UTF-8인 `scripts/meal-update-prompt.md`에 두고 스크립트에서 읽어 쓴다. 스크립트 본문에는 한글을 넣지 않는다.
